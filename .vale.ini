StylesPath = styles
MinAlertLevel = warning

# Scoped to Blueprints only (Reference-type docs, rules/documentation_standard.md §1).
# ADRs, Walkthroughs, and Guides are expected to contain this vocabulary --
# flagging them would penalize the exact content they exist to hold.
[*_BLUEPRINT.md]
BasedOnStyles = Diataxis
